print('Vamos fazer um calculos de somas simples.')
print('Para isso preciso que vc digite 2 (dois) numeros que ira ser somado')

n1 = int(input('Digite um numero:'))
n2 = int(input('Digite outro numero:'))
s = n1 + n2

print('Somei os numero para vc.')
print('A soma entre {} e {} é exatamente:{}.'.format(n1, n2, s))
### Nessa aula, aprendi a usar a sintaxe de formato para simplificar codigos.
# ".format serve para que {} tenha uma identificacao das variaveis podendo ser identificadas
# e a ordem que deve ser colocada. deve ficar entre parenteses e separa as variaveis com virgula. ###
